fn factorial(n: int) -> int {
    let result: int = 1;
    let i: int = 0;

    while i < n {
        i = i + 1;
        result = result * i;
    }

    return result;
}

fn main() {
    printi(factorial(5)); // 120
    return;
}
